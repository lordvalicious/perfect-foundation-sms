# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
DJANGO_TIME_ZONE=UTC

# Database (local: Postgres via docker-compose.yml)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=perfect_foundation
DB_USER=school_admin
DB_PASSWORD=school_password
DB_HOST=localhost
DB_PORT=5432

# Email
DEFAULT_FROM_EMAIL=no-reply@perfectfoundation.edu

# Optional: use SQLite for a quick local run without Docker
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=C:\Users\Ryuk\Documents\perfect-foundation-sms\backend\db.sqlite3

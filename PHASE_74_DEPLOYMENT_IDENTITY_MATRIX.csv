PHASE 74 — DEPLOYMENT IDENTITY MATRIX
=======================================

This matrix documents the production deployment identity for each component
of the Perfect Foundation SMS system. Based on evidence available as of
2026-09-23. All unverified items marked UNVERIFIED.

component,production_url,vercel_project,deployment_id,deployment_url,created_at,git_repository,git_branch,git_commit_sha,deployment_state,evidence,status,notes

FRONTEND,https://perfect-foundation-sms.vercel.app,perfect-foundation-sms,UNVERIFIED,https://perfect-foundation-sms.vercel.app,UNVERIFIED,https://github.com/lordvalicious/perfect-foundation-sms,master,4112ad5,UNVERIFIED,Cannot obtain from current environment (no Vercel CLI/dashboard access). Health endpoint returns 200. Frontend rewrites /api/ to API URL.,UNVERIFIED,Deployment ID not exposed by Vercel. Commit SHA 4112ad5 is known from repository but not proven deployed.

API,https://perfect-foundation-api.vercel.app,perfect-foundation-api,UNVERIFIED,https://perfect-foundation-api.vercel.app,UNVERIFIED,https://github.com/lordvalicious/perfect-foundation-sms,master,4112ad5,UNVERIFIED,Cannot obtain from current environment (no Vercel CLI/dashboard access). Health endpoint returns 200. Frontend rewrites /api/ to this URL.,UNVERIFIED,Deployment ID not exposed by Vercel. Commit SHA 4112ad5 is known from repository but not proven deployed.

/api/deploy-test,https://perfect-foundation-api.vercel.app/api/deploy-test,N/A,UNVERIFIED,https://perfect-foundation-api.vercel.app/api/deploy-test,UNVERIFIED,N/A,UNVERIFIED,UNVERIFIED,HTTP 404 on both frontend and backend. Route exists in source code (backend/config/urls.py:81, DeployTestView) but not in deployed artifact.,UNVERIFIED,404 means route not in deployed serverless artifact or git command fails. /api/health/ works on same prefix, so routing is not broadly blocked.

FRONTEND→API WIRING, N/A, N/A, N/A, N/A, N/A, N/A, N/A, N/A, Frontend vercel.json rewrites /api/:path to https://perfect-foundation-api.vercel.app/api/:path. Backend DJANGO_ALLOWED_HOSTS includes perfect-foundation-sms.vercel.app. Both health endpoints return 200.,UNVERIFIED,Frontend→API wiring confirmed at Vercel rewrite level. /api/deploy-test/ 404 cause unresolved (deployment artifact issue).

==========================================================================
MATRIX STATUS LEGEND
==========================================================================

status column values:
- PROVEN — deployment identity definitively established with direct evidence
- VERIFIED — deployment confirmed via reliable evidence (less than PROVEN but sufficient)
- UNVERIFIED — cannot prove deployment identity from current environment
- N/A — not applicable

evidence column values:
- Direct HTTP response evidence (health endpoint 200)
- Source code configuration (vercel.json, urls.py)
- Git repository analysis (HEAD commit known)
- Vercel dashboard data (not accessible)
- Not obtainable from current environment

==========================================================================
KEY FINDINGS FROM MATRIX
==========================================================================

1. Both frontend and API deployments have UNVERIFIED deployment IDs
   - Cannot obtain without Vercel dashboard access or Vercel CLI
   - URLs are confirmed reachable (health endpoints return 200)
   - Git HEAD commit (4112ad5) is known from repository but not proven deployed

2. /api/deploy-test/ returns HTTP 404 on both frontend and backend
   - Route exists in source code (confirmed in backend/config/urls.py)
   - Route NOT in deployed artifact (404 proves this)
   - Cause: deployment artifact stale/incomplete (most probable)
   - NOT blocked at HTTP level (/api/health/ works on same /api/ prefix)

3. Frontend→API wiring is CONFIRMED:
   - Frontend/vercel.json rewrites /api/:path → https://perfect-foundation-api.vercel.app/api/:path
   - Backend/.env.production DJANGO_ALLOWED_HOSTS includes perfect-foundation-sms.vercel.app
   - Both /api/health/ endpoints return 200
   - Wiring is functional; only /api/deploy-test/ marker is unverifiable

4. Frontend and API revision match: UNVERIFIED
   - Cannot prove which commit SHA is deployed to each
   - Both point to same Git HEAD (4112ad5) in repository
   - But cannot prove either is the revision serving production

5. /api/deploy-test/ root cause: DEPLOYMENT_ARTIFACT_STALE_OR_INCOMPLETE
   - Most probable: Vercel Python serverless artifact does not include the urlpattern
   - Alternative: git rev-parse HEAD fails in serverless environment
   - NOT an application defect; NOT a routing blanket-block

==========================================================================
 NEXT REQUIRED ACTIONS
==========================================================================

1. Obtain Vercel dashboard access to:
   a. Retrieve deployment IDs for perfect-foundation-sms and perfect-foundation-api
   b. Verify commit SHAs for each deployment
   c. Check whether /api/deploy-test/ is included in deployed functions
   d. Redeploy if necessary to include /api/deploy-test/

2. After Step 1, determine:
   a. Whether frontend and backend serve matching revisions
   b. Whether /api/deploy-test/ returns PHASE66_DEPLOYMENT_PROOF_20260923
   c. R73-P0-001 status: RESOLVED or remains OPEN

3. Obtain browser automation capability for R73-P0-002 (independent of R73-P0-001)

==========================================================================
MATRIX CREATION DATE: 2026-09-23
CREATED BY: Phase 74 Deployment Identity Investigation
ALL STATEMENTS BASED ON EVIDENCE AVAILABLE AS OF THIS DATE
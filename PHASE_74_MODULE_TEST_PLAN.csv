PHASE 74 MODULE TEST PLAN
=======================

This test plan defines exactly what must be tested for each module during
Phase 74. Based entirely on Phase 73 master feature matrix evidence.
No module certified PASS without direct production evidence. No module
claimed as "working" without deployment + authentication + read-only test.

MODULES_FROM_PHASE_73_MASTER_MATRIX (identified in Phase 73 audit):
Library, Reports, Visitors, Payroll, Health Records, Students, Attendance,
Fees, Academics, Timetable, Exams, Admissions, SMS, Email, Hostel,
Transport, Library Issues, Library Reservations, Library Members,
Library Settings, Assessments, Conduct, Results, Archive

TEST_PLAN_PER_MODULE (CSV format):
MODULE,SOURCE_IMPLEMENTED,DEPLOYED_PRODUCTION,ROLE_TESTED,LOGIN_STATUS,MODULE_ACCESS,ACCESS_STATUS,READ_ONLY_OPERATION,READ_ONLY_STATUS,READ_ONLY_EVIDENCE,FAILURE_REASON,STATUS,NOTES

DEFINITIONS:
- SOURCE_IMPLEMENTED: YES if module present in source code (from Phase 73
  master feature matrix code inspection). Does NOT mean deployed or working.
- DEPLOYED_PRODUCTION: YES if module present in deployed production revision.
  NO if /api/<module>/ returns 404 or module not found. UNVERIFIED if
  deployment identity not yet established (before Phase 74 Step 1).
- ROLE_TESTED: Role used to test this module (from Phase 73 role×matrix).
  Appropriate role for module type.
- LOGIN_STATUS: 200 (login succeeded), 401 (unauthorized), 403 (forbidden),
  UNVERIFIABLE (could not login), N/A (module not accessible for this role)
- ACCESS_STATUS: 200 (module accessible), 403 (forbidden role), 404 (not found),
  500 (server error), UNVERIFIABLE (could not test), N/A
- READ_ONLY_OPERATION: Safe read-only operation (list, view details, search,
  filter — NOT create, edit, delete)
- READ_ONLY_STATUS: PASS (operation succeeded with expected data), FAIL
  (operation failed — 404, 500, authorization error), UNVERIFIABLE (could
  not test), N/A (no read-only operation defined for this module)
- STATUS: FINAL status for this module — one of:
  PASS (all read-only operations passed in production),
  FAIL (any read-only operation failed, or module completely inaccessible),
  UNVERIFIABLE (could not test due to environment limitations — NOT a failure),
  IMPLEMENTED_BUT_NOT_CERTIFIED (source implemented but not proven in production)

PER-MODULE PROCEDURE:

1. DETERMINE SOURCE_IMPLEMENTED
   - Check Phase 73 master feature matrix
   - Record YES/NO
   - This step documents what exists in code, NOT what works in production

2. DETERMINE DEPLOYED_PRODUCTION
   - After Phase 74 Step 1 (deployment identity established):
   - Call /api/<module>/ on production backend
   - Record HTTP status and response
   - If deployment identity NOT yet established: record UNVERIFIED
   - YES = module responds with expected data (200)
   - NO = module returns 404 or entirely different response
   - UNVERIFIED = deployment identity not yet known

3. IDENTIFY APPROPRIATE ROLE
   - From Phase 73 role×module matrix
   - Assign role that should have access to this module
   - Example: Library → LIBRARIAN, Reports → ACCOUNTANT or HR,
     Visitors → GUARD, Health Records → NURSE, Payroll → HR

4. PERFORM LOGIN WITH ASSIGNED ROLE
   - Use Phase 74 authenticated testing (Step 2)
   - Record LOGIN_STATUS
   - If login fails: record LOGIN_STATUS and mark module UNVERIFIABLE
   - If login succeeds: proceed to module access test

5. TEST MODULE ACCESS
   - Navigate to /api/<module>/ or appropriate endpoint
   - Record HTTP status code
   - Record response snippet (first 200 characters)
   - Determine ACCESS_STATUS:
     - 200 → PASS (module accessible)
     - 403 → FAIL (role forbidden, or authorization defect)
     - 404 → FAIL (module not deployed or wrong route)
     - 500 → FAIL (server error in module)
     - Other → document
     - If cannot test: UNVERIFIABLE

6. PERFORM READ-ONLY OPERATION
   For each module, define and perform safe read-only operation:
   - Library: List books (GET /api/library/), search books, view book details
     (GET /api/library/books/{id}) — NOT issue/return
   - Reports: List reports (GET /api/reports/), view report details
   - Visitors: List visitors (GET /api/visitors/), view visitor details
   - Payroll: List payroll items (GET /api/payroll/), view item details
   - Health Records: List records (GET /api/health-records/), view details
   - Students: List students (GET /api/students/), view student details
   - Attendance: List attendance (GET /api/attendance/), view records
   - Fees: List fees (GET /api/fees/), view fee details
   - Academics: View grades, view course listings
   - Timetable: View timetable, view schedule
   - Exams: View exam schedule, view exam details
   - Admissions: View admissions applications, view application details
   - SMS: View SMS messages (if read-only), view message history
   - Email: View sent emails (if read-only), view email history
   - Hostel: View room assignments, view room details
   - Transport: View vehicle assignments, view route details
   - Library Issues: NOT testable (requires data mutation — record as
     IMPLEMENTED_BUT_NOT_CERTIFIED with mutation blocker noted)
   - Library Reservations: NOT testable (requires data mutation)
   - Library Members: List members (GET /api/library/members/), view details
   - Library Settings: View settings (GET /api/library/settings/)
   - Assessments: View assessment lists, view details
   - Conduct: View conduct records, view details
   - Results: View result sets, view student results
   - Archive: View archived records (read-only)

   For each read-only operation:
   - Perform the GET request
   - Record HTTP status code
   - Record response snippet (first 200 chars)
   - Determine if expected data is present
   - Record READ_ONLY_STATUS: PASS, FAIL, UNVERIFIABLE, N/A

7. DETERMINE FINAL STATUS
   - PASS: All tested read-only operations returned 200 with expected data
   - FAIL: Any read-only operation returned 403/404/500, or module
     completely inaccessible (404 on list endpoint)
   - UNVERIFIABLE: Could not test (no authenticated session, deployment
     identity unknown, module not accessible for any role)
   - IMPLEMENTED_BUT_NOT_CERTIFIED: Source code has module (SOURCE_IMPLEMENTED=YES)
     but DEPLOYED_PRODUCTION=NO or module could not be tested with any role

8. RECORD NOTES
   - Any anomalies observed
   - Failure reasons if applicable
   - Role used for testing
   - Any other relevant evidence

CSV HEADER:
MODULE,SOURCE_IMPLEMENTED,DEPLOYED_PRODUCTION,ROLE_TESTED,LOGIN_STATUS,MODULE_ACCESS_ENDPOINT,MODULE_ACCESS_STATUS,ACCESS_STATUS_DETAIL,READ_ONLY_OPERATION,READ_ONLY_1_OPERATION,READ_ONLY_1_STATUS,READ_ONLY_1_EVIDENCE,READ_ONLY_2_OPERATION,READ_ONLY_2_STATUS,READ_ONLY_2_EVIDENCE,READ_ONLY_3_OPERATION,READ_ONLY_3_STATUS,READ_ONLY_3_EVIDENCE,FAILURE_REASON,FINAL_STATUS,NOTES

EXAMPLE ROW — Library (theoretical):
Library,YES,YES,LIBRARIAN,200,/api/library/,200,accessible,List books,200,["id","title","author"] present,Search books,200,["results"] present,View details,200,book details present,N/A,PASS,Library fully functional in production deployment

EXAMPLE ROW — Payroll (theoretical):
Payroll,YES,YES,HR,200,/api/payroll/,200,accessible,List payroll items,200,["id","employee_name","amount"] present,N/A,N/A,N/A,N/A,N/A,Payroll processing requires data mutation — IMPLEMENTED_BUT_NOT_CERTIFIED per PHASE_73_DATA_MUTATION_BLOCKERS,IMPLEMENTED_BUT_NOT_CERTIFIED,Mutation required for full certification

EXAMPLE ROW — Library Issues (theoretical):
Library Issues,YES,UNVERIFIABLE,UNVERIFIABLE,UNVERIFIABLE,/api/library/issues/,UNVERIFIABLE,requires_issue_issue_management_issue,Issue/return book,UNVERIFIABLE,requires_data_mutation,Requires data mutation per PHASE_73,IMPLEMENTED_BUT_NOT_CERTIFIED,N/A

ALL ENTRIES BASED ON DIRECT EVIDENCE FROM DEPLOYED PRODUCTION SYSTEM
AFTER PHASE 74 STEPS 1-2 (deployment identity + browser authentication).
NO CLAIMS WITHOUT EVIDENCE. NO PASS FROM SOURCE CODE ALONE.
IMPLEMENTED_BUT_NOT_CERTIFIED is an acceptable final status for modules
that exist in source but are not yet proven in production.
# AI Gap Report — Perfect Foundation SMS

Generated by `tools/gap_analyzer.py` · 2026-09-06 00:53

## 1. Inventory snapshot

- Backend apps: **35** · models/classes: **184**
- Backend URL routes: **579**
- Frontend pages: **67** · routed paths: **55**
- Frontend-distinct API calls: **124**
- Orphaned frontend pages: **PendingApprovalsPage, ReportsCenter, WorkflowDefinitionAdminPage, WorkflowInstanceDetailPage**

## 2. Integrations detected

- **2FA / TOTP** (`backend/apps/accounts/tests.py`)
- **Google SSO** (`backend/apps/accounts/urls.py`)
- **SMS provider (Twilio)** (`backend/apps/communication/sms.py`)
- **wallet: JazzCash** (`backend/apps/finance/easypaisa_views.py`)
- **wallet: EasyPaisa** (`backend/apps/finance/easypaisa_views.py`)
- **online payment (Stripe)** (`backend/apps/finance/jazzcash_views.py`)
- **PDF generation** (`backend/apps/finance/pdf.py`)

## 3. Capability checklist

| Category | Status | Signals | Recommendation |
| --- | --- | --- | --- |
| AI & smart features | missing (0/3) | — | ADD - No AI anywhere today; AI drafts feedback, alerts, reports, and smart search. |
| Online payment (wallets) | partial (3/5) | wallet: JazzCash, wallet: EasyPaisa, stripe | ADD - JazzCash/EasyPaisa backends are complete but not wired into the UI. |
| Workflow / approvals | partial (2/3) | workflow, workflow | ADD - Workflow engine is complete but its UI is unrouted; surfaces approvals per role. |
| Notifications & messaging | complete (4/4) | communication, SMS provider (Twilio), notifications, communication | OK |
| Reporting & analytics | partial (3/4) | reports, reports, dashboard | ADD - ~150 endpoints + builder exist; reduce to the surfaced catalog (ReportsCenter). |
| Auth & security | complete (4/4) | accounts, 2FA / TOTP, Google SSO, 2fa | OK |
| Finance automation | complete (4/4) | finance, finance, finance, PDF generation | OK |
| Student/parent mobile | complete (3/3) | parent-portal, sw.js, portal | OK |
| Timetable management | complete (3/3) | timetable, timetable, timetable | OK |
| Data import / export | partial (1/3) | reports | ADD - Extend UI import beyond students/teachers to the backend catalog. |

## 4. What to ADD

- **JazzCash/EasyPaisa backends built but unused by the UI** — Wire to UI or remove the labels; server code is complete (hash + callbacks).
- **Workflow engine UI orphaned (no routes/nav)** — Surface PendingApprovals/definitions; engine is done, navigation is missing.
- **No AI features in frontend or backend** — Add AI drafting/smart-search/prediction layers on existing data models.
- **Email delivery depends on DJANGO_EMAIL_* (unset)** — Configure an SMTP send provider or document SMS-first messaging.
- **Frontend data import covers only students+teachers** — Extend to the backend's import catalog; no new engine needed.

## 5. What NOT to add (cut / avoid)

- **ReportsCenter.jsx unreferenced dead code** — Pick ReportsPage or ReportsCenter catalog; ship one, delete the other.
- **Stripe is the only wired online payment path** — Finish J/E wallets first; avoid adding more providers.
- **Multi-tenancy + campus isolation already enforced in ORM** — Do not redesign the tenancy layer.
- **Auth stack is comprehensive (SSO, 2FA, lockout, password history)** — Do not build another auth system.

## 6. Method

Inventory is parsed statically (routes, models, endpoints, API calls, integrations). Heuristic scoring compares signals per capability. In `--llm` mode an LLM refines the recommendations from this same evidence.

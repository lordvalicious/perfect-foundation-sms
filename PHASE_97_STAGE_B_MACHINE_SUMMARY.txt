PHASE 97 STAGE B MACHINE SUMMARY
================================

REPO CONTEXT
------------
HEAD: cd00325 (test(e2e): wire five-role session lookup for Phase 96.8 roles)
BRANCH: origin/master (tracked worktree clean, only untracked phase deliverables)
BASELINE: 7357c18d1e4352bdce41b7de23c36eead4b66681 (13 commits ancestor)
MIGRATION 0028: ABSENT, graph unchanged since baseline

AUTHORIZATION SOURCE-TRUTH ANALYSIS (frontend/src/App.jsx)
----------------------------------------------------------
Roles traced from navigation array + route-level RequireRoles gates:

  COUNSELLOR    -> Helpdesk (nav + route both include "counsellor")         : ESTABLISHED
  GUARD         -> Helpdesk + Visitors (nav + route include "guard")          : ESTABLISHED
  NURSE         -> Health Records (nav + route include "nurse")               : ESTABLISHED
  ADMINISTRATIVE_OFFICER -> Helpdesk (nav + route include "administrative_officer") : ESTABLISHED
  LIBRARIAN     -> Library (nav shows "library" but NO /library route in App.jsx) : NOT ESTABLISHED

NOT ESTABLISHED items (per directive: do not invent):
  - /library route for LIBRARIAN (route absent)
  - /visitors for counsellor, administrative_officer, nurse, librarian (explicitly gated)
  - /health-records for counsellor, administrative_officer, guard, librarian (explicitly gated)
  - /digital-ids for any of the 5 roles (all excluded by role list)
  - Backend API permission classes for helpdesk/visitors/health-records/library
  - ROLE_GRANTS for counsellor, nurse, administrative_officer in demo_seed
  - Frontend primary_role/has_any_role/ROLE_ACCESS references for the five roles

SESSION ACQUISITION STRATEGY
----------------------------
Option A: Runtime login at test startup using Phase 96.9 credential file
  (p969_stage_b_credentials.env via POST /api/auth/login/ per role).
  Aligns with existing harness (P43_<ROLE>_SESSIONID env, getSessionId()).
  Passwords remain outside repo.

Option B: Temporary runtime session files (Netscape format) in P43_SESSIONS_DIR.
  Also outside repo. No scripts provided in this preflight.

RECOMMENDATION: Option A (lower-friction, env-driven, no temp files).

PROPOSED TEST SCOPE (derived deterministically)
----------------------------------------------
5 role-spec files + ~60 test() calls (core identity/role/scope/per-role authz)
+ 5 nav-visibility assertions + 4 route-access block tests = ~70 total
LIBRARIAN /library test: nav visibility only + attempt route to document block.

PRODUCTION-SAFETY FINDINGS
--------------------------
- No destructive ops in e2e suite (only UI clicks on empty login form, nav toggles)
- Five accounts active; re-login verified via GET /api/auth/me/
- Frontend proxy /api/health/ -> 200; CSP connect-src matches target
- No hard-coded creds in repo (.env.example gitignored; .env files runtime-only)
- Session strategy A keeps passwords outside repo; B uses temp files also outside repo

FINAL GATE DETERMINATION
------------------------
PHASE 97 STAGE B PREFLIGHT FINAL GATE: UNKNOWN
(requiring owner implementation review)

RATIONALE: Source establishes partial module/authorization expectations
for 4 of 5 roles via navigation + /helpdesk gate. One critical gap:
/library route absent for LIBRARIAN, making that expectation NOT
ESTABLISHED. Session strategy not decided. Owner must review and either
(1) authorize implementation to fill gaps, or (2) confirm scope reduction.

DELIVERABLES (untracked, no secrets)
-------------------------------------
  PHASE_97_STAGE_B_PREFLIGHT.md          (this file's content source)
  PHASE_97_STAGE_B_PREFLIGHT_MATRIX.csv
  PHASE_97_STAGE_B_MACHINE_SUMMARY.txt

STOP: Do NOT implement, modify source, create sessions, or execute E2E.
Wait for separate owner approval.